# Code QR / โคตร QR

Next.js 14 app with Supabase. Copy `.env.example` to `.env.local` and fill in keys.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

Design tokens live in `design.md` and `tailwind.config.ts`.
